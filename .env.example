# Hyperliquid API認証情報（必須）
HYPERLIQUID_PRIVATE_KEY=0x...  # APIウォレットの秘密鍵（0x+64文字の16進数）
HYPERLIQUID_API_WALLET_ADDRESS=0x...  # APIウォレットのアドレス
HYPERLIQUID_MAIN_WALLET_ADDRESS=0x...  # メインウォレットのアドレス（資金のあるウォレット）

# Discord通知用Webhook URL（必須）
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# トレード設定
MONITORING_INTERVAL=10  # 市場データ取得間隔（秒）: 10秒ごとに監視
ORDER_TIMEOUT=600  # 未約定注文の自動キャンセル時間（秒）: 10分でキャンセル
POSITION_CLOSE_TIMEOUT=1800  # ポジション自動クローズ時間（秒）: 30分でクローズ
PRICE_MULTIPLIERS=2.0  # 価格倍率（カンマ区切り）: >1.0=売り注文、<1.0=買い注文
ORDER_AMOUNTS_USDC=100  # 各注文のサイズ（USDC建て、カンマ区切り）: 倍率と同数指定
MAX_CONCURRENT_ORDERS=1  # 同時保有可能な銘柄数上限: 1 = 1銘柄のみ（安全）

# 監視銘柄フィルター（カンマ区切り、空欄 = 全銘柄監視）
# 例: BTC,ETH,SOL,DOGE
# 空欄の場合は200以上の全銘柄を監視
SYMBOLS=BTC,ETH,SOL
# 異常検知パラメータ
DETECTOR_WINDOW_SIZE=60  # 統計計算用の履歴サンプル数: 60サンプル（10秒×60=10分のデータ）
VOLUME_Z_THRESHOLD=3.0  # 出来高異常のZ-score閾値: 3.0 = 3σ異常（99.7%信頼区間外）
PRICE_Z_THRESHOLD=3.0  # 価格異常のZ-score閾値: 3.0 = 3σ異常（99.7%信頼区間外）
DETECTION_MODE=vol_only  # 検知モード: vol_only=出来高のみ, price_only=価格のみ, vol_and_price=両方必要, vol_or_price=どちらか